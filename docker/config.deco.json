{ 
  "listenAddress": ":8080",
  "account": {
    "region": "us-east-1",
    "akid": "{{ .akid }}",
    "secret": "{{ .secret }}",
    "externalId": "{{ .external_id }}",
    "role": "{{ .cross_account_role }}"
  },
  "token": "{{ .api_token }}",
  "logLevel": "{{ .log_level }}",
  "org": "{{ .spinup_org }}"
}
